# informe 
 
 // Jaksaako joku oikeesti kommentoida
 
 rahe = getOpintolaina();
 
 if(rahe > 0) {
  buyKalja(rahe, out kaljat);
 }
 
 foreach(kalja k in kaljat) {
  drinkKalje(k);
   if(humala == "suuri") {
    passout();
    break;
  }
 }
 
 kys();

# informe
