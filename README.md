# informe 
 
 // Jaksaako joku oikeesti kommentoida
 
 rahe = getOpintolaina();
 
 if(rahe > 0) {</br>
  buyKalja(rahe, out kaljat);</br>
 }
 
 foreach(kalja k in kaljat) {</br>
  drinkKalje(k);</br>
   if(humala == "suuri") {</br>
    passout();</br>
    break;</br>
  }</br>
 }
 
 kys();

# informe
